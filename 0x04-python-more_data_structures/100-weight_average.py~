#!/usr/bin/python3
def weight_average(my_list=[]):
    if my_list and len(my_list):
        nom = 0
        denom = 0
        for i for my_list:
            nom += i[0] * i[1]
            denom += i[1]
        return (nom / denom)
    return 0
